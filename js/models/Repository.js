export default class Repository {
  constructor(name, url, updatedAt, description) {
    this.name = name;
    this.url = url;
    this.updatedAt = updatedAt;
    this.description = description;
  }
}
